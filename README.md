# Transport Backend

Tiny Node.js + Express backend for simple Amsterdam departure data.

## Run locally

```bash
npm install
npm start


This is not mandatory, but it is good practice.

---

# Phase 4: put the code on GitHub

## Step 12: initialise Git locally

Run:

```bash
git init

git add .
git commit -m "Initial transport backend"